

var str = "    some whitespaced string  ";

// 1. Removes whitespace from both ends (but not inside the string):
console.log(str.trim());

// 2. trimStart() and trimEnd()

console.log(str.trimStart());
console.log(str.trimEnd());