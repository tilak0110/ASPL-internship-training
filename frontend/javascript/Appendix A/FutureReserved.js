// enum, await, implements, interface, package,
// private, protected, public, static, let
//💡 await is already in use inside async functions.


let pkg = "my-package";
let isPrivate = false;
let pubInterface = { foo: "bar" };

console.log(pkg, isPrivate, pubInterface);
