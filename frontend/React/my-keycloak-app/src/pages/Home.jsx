import { Typography, Container } from '@mui/material';

const Home = () => (
  <Container>
    <Typography variant="h4" gutterBottom>
      Welcome to the Home Page (Public)
    </Typography>
  </Container>
);

export default Home;