import React from "react";

const NoPropsComponent: React.FC = () => {
  return <p>No props needed here.</p>;
};
// const NoPropsComponent = (): JSX.Element => {
//   return <p>No props needed here.</p>;
// };

export default NoPropsComponent;